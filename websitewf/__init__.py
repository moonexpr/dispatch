"""websitewf — the WebsiteWF engine package (an overlay over baseworkflow).

This ``__init__`` makes ``websitewf`` a regular package so the name resolves to
this directory rather than the sibling ``websitewf.py`` module when a test is run
from inside the package. The public engine entry points live in
``websitewf.websitewf`` and ``websitewf.bindings``.
"""
